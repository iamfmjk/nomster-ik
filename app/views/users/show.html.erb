<div class="col-10 offset-1">
  <div class="booyah-box">
    <div class="row">
      <div class="col-4">
        <%= image_tag 'user.png' %>
      </div>
      <div class="col-8">
        <h1><%= @user.email %></h1>
        <h3>Member since: <%= @user.created_at.strftime("%B %d, %Y") %></h3>
        <h4><%= @user.comments.count %> total comments</h4>
        <h4><%= @user.comments.where(rating: '5_stars').count %> total 5 star reviews</h4>
      </div>
    </div>
    <div class="clear"></div>
  </div>

  <h1 class="text-center mb-3 mt-3">My Comments</h1>
  <div class="row">
    <% @user.comments.each do |comment| %>
      <div class="col-12 col-sm-6">
        <h3><%= link_to comment.place.name, place_path(comment.place) %></h3>
        <blockquote>
          <% if comment.humanized_rating.present? %>
              <b><%= comment.humanized_rating %></b>
              <br />
          <% end %>
          <%= comment.message %>
            <small><%= comment.user.email %></small>
        </blockquote>
        <%= link_to 'Delete comment', place_comment_path(comment.place), class: 'delete-link', method: :delete, data: {confirm: 'Are you sure you want to delete this?'} %>
      </div>
    <% end %>
  </div>
</div>